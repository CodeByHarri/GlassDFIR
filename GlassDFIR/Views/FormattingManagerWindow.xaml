<Window x:Class="GlassDFIR.Views.FormattingManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GlassDFIR.Views"
        mc:Ignorable="d"
        Title="Manage Formatting Rules" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource SurfaceBrush}">
    <Window.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                         <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                         <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Active Formatting Rules" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="{StaticResource SurfaceLightBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
            <ListBox x:Name="RulesList" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/> <!-- Color Swatch -->
                                <ColumnDefinition Width="*"/>    <!-- Description -->
                                <ColumnDefinition Width="Auto"/> <!-- Delete Button -->
                            </Grid.ColumnDefinitions>

                            <Border Width="16" Height="16" CornerRadius="8" Background="{Binding ColorHex}" VerticalAlignment="Center" Margin="0,0,10,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"/>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding PropertyName}" Foreground="{StaticResource SecondaryTextBrush}" FontSize="12"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Contains: " Foreground="{StaticResource PrimaryTextBrush}" FontSize="14"/>
                                    <TextBlock Text="{Binding Value}" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="SemiBold" FontSize="14"/>
                                </StackPanel>
                            </StackPanel>

                            <Button Grid.Column="2" Content="🗑️" Click="DeleteRule_Click" Tag="{Binding}"
                                    Width="30" Height="30" Background="Transparent" BorderThickness="0" 
                                    Foreground="{StaticResource SecondaryTextBrush}" Cursor="Hand"
                                    ToolTip="Remove Rule">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="#f7768e"/> <!-- Red on hover -->
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Reset to Defaults" Click="ResetDefaults_Click" Background="Transparent" BorderBrush="#7aa2f7" BorderThickness="1" Foreground="#7aa2f7" Padding="15,6" Margin="0,0,10,0" ToolTip="Restore standard Hayabusa color coding"/>
            <Button Content="Clear All Rules" Click="ClearAll_Click" Background="Transparent" BorderBrush="#f7768e" BorderThickness="1" Foreground="#f7768e" Padding="15,6" Margin="0,0,10,0"/>
            <Button Content="Close" Click="Close_Click" Background="{StaticResource PrimaryActionBrush}" Foreground="#1a1b26" Padding="20,6" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
