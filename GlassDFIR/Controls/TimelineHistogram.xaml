<UserControl x:Class="GlassDFIR.Controls.TimelineHistogram"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GlassDFIR.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800"
             Height="100">
    <UserControl.Resources>
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" 
                        Grid.ColumnSpan="2"
                        CornerRadius="4"
                        Background="#1a1b26"
                        BorderBrush="#414868"
                        BorderThickness="1" />
                <Border Grid.Column="0"
                        CornerRadius="4,0,0,4" 
                        Margin="1" 
                        Background="#1a1b26" 
                        BorderBrush="Transparent" 
                        BorderThickness="0,0,1,0" />
                <Path x:Name="Arrow"
                      Grid.Column="1"     
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Data="M0,0 L0,2 L4,6 L8,2 L8,0 L4,4 z"
                      Fill="#7aa2f7" />
            </Grid>
        </ControlTemplate>

        <ControlTemplate x:Key="ComboBoxTextBox" TargetType="TextBox">
            <Border x:Name="PART_ContentHost" Focusable="False" Background="{TemplateBinding Background}" />
        </ControlTemplate>

        <Style x:Key="DarkComboBox" TargetType="{x:Type ComboBox}">
            <Setter Property="Foreground" Value="#c0caf5"/>
            <Setter Property="Background" Value="#1a1b26"/>
            <Setter Property="BorderBrush" Value="#414868"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="MinHeight" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          Template="{StaticResource ComboBoxToggleButton}" 
                                          Grid.Column="2" 
                                          Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite"
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="6,3,23,3"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left" />
                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}" 
                                     Template="{StaticResource ComboBoxTextBox}" 
                                     HorizontalAlignment="Left" 
                                     VerticalAlignment="Center" 
                                     Margin="6,3,23,3"
                                     Focusable="True" 
                                     Background="Transparent"
                                     Foreground="#c0caf5"
                                     Visibility="Hidden"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" 
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="#1a1b26"
                                            BorderThickness="1"
                                            BorderBrush="#414868"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#565f89"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ComboBoxItem}">
             <Setter Property="SnapsToDevicePixels" Value="True"/>
             <Setter Property="Foreground" Value="#c0caf5"/>
             <Setter Property="Background" Value="Transparent"/>
             <Setter Property="BorderBrush" Value="Transparent"/>
             <Setter Property="BorderThickness" Value="0"/>
             <Setter Property="Padding" Value="6,4"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                         <Border Name="Bd"
                                 Background="{TemplateBinding Background}"
                                 BorderBrush="{TemplateBinding BorderBrush}"
                                 BorderThickness="{TemplateBinding BorderThickness}"
                                 Padding="{TemplateBinding Padding}"
                                 SnapsToDevicePixels="true">
                             <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                               VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                               SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsHighlighted" Value="true">
                                 <Setter TargetName="Bd" Property="Background" Value="#414868"/>
                                 <Setter Property="Foreground" Value="#ffffff"/>
                             </Trigger>
                             <Trigger Property="IsEnabled" Value="false">
                                 <Setter Property="Foreground" Value="#565f89"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

        <!-- Calendar Day Button Style -->
        <Style x:Key="DarkCalendarDayButton" TargetType="{x:Type CalendarDayButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#c0caf5"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarDayButton">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#292e42"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#7aa2f7"/>
                                <Setter Property="Foreground" Value="#15161e"/>
                            </Trigger>
                            <Trigger Property="IsToday" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#7aa2f7"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsBlackedOut" Value="True">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <Trigger Property="IsInactive" Value="True">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="Foreground" Value="#565f89"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Calendar Button Style (Months/Years) -->
        <Style x:Key="DarkCalendarButton" TargetType="{x:Type CalendarButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#c0caf5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarButton">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#292e42"/>
                            </Trigger>
                            <Trigger Property="HasSelectedDays" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3b4261"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Main Calendar Style -->
        <Style x:Key="DarkCalendar" TargetType="{x:Type Calendar}">
            <Setter Property="Background" Value="#1a1b26"/>
            <Setter Property="BorderBrush" Value="#414868"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#c0caf5"/>
            <Setter Property="CalendarDayButtonStyle" Value="{StaticResource DarkCalendarDayButton}"/>
            <Setter Property="CalendarButtonStyle" Value="{StaticResource DarkCalendarButton}"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <!-- DatePicker TextBox Style -->
        <Style TargetType="{x:Type DatePickerTextBox}">
            <Setter Property="Background" Value="#1a1b26"/>
            <Setter Property="Foreground" Value="#c0caf5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="2,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DatePickerTextBox}">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkDatePicker" TargetType="{x:Type DatePicker}">
            <Setter Property="Foreground" Value="#c0caf5"/>
            <Setter Property="Background" Value="#1a1b26"/>
            <Setter Property="BorderBrush" Value="#414868"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="SelectedDateFormat" Value="Short"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="CalendarStyle" Value="{StaticResource DarkCalendar}"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#1a1b26">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header / Controls -->
            <RowDefinition Height="*"/>    <!-- Chart Area -->
            <RowDefinition Height="Auto"/> <!-- Axis Labels -->
        </Grid.RowDefinitions>

        <!-- Configuration Bar -->
        <Border Grid.Row="0" Padding="5,4" Background="#16161e" BorderBrush="#1a1b26" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Timeline Context" Foreground="#7aa2f7" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="DateColumnSelector" Width="180" SelectionChanged="DateColumnSelector_SelectionChanged"
                          Style="{StaticResource DarkComboBox}"/>
                
                <!-- Date Pickers -->
                <DatePicker x:Name="StartDatePicker" Width="100" Margin="10,0,0,0" SelectedDateChanged="DatePicker_SelectedDateChanged" VerticalAlignment="Center" Style="{StaticResource DarkDatePicker}"/>
                <TextBlock Text="-" Foreground="#565f89" VerticalAlignment="Center" Margin="5,0"/>
                <DatePicker x:Name="EndDatePicker" Width="100" Margin="0,0,0,0" SelectedDateChanged="DatePicker_SelectedDateChanged" VerticalAlignment="Center" Style="{StaticResource DarkDatePicker}"/>

                <!-- Zoom Controls -->
                <Button Content="-" Click="ZoomIn_Click" Width="30" Height="30" Padding="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Background="#24283b" BorderBrush="#414868" BorderThickness="1" Foreground="#c0caf5" Margin="10,0,0,0" ToolTip="Zoom In" FontSize="16" FontWeight="Bold"/>
                <Button Content="+" Click="ZoomOut_Click" Width="30" Height="30" Padding="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Background="#24283b" BorderBrush="#414868" BorderThickness="1" Foreground="#c0caf5" Margin="4,0,0,0" ToolTip="Zoom Out" FontSize="16" FontWeight="Bold"/>

                <!-- Error Display -->
                <TextBlock x:Name="DateValidationError" Text="Start date must be before end date" Foreground="#f7768e" FontSize="11" VerticalAlignment="Center" Margin="15,0,0,0" Visibility="Collapsed" FontWeight="SemiBold"/>

                <!-- Reset / Status -->
                <Button x:Name="ResetBtn" Content="Reset" Click="ResetFilter_Click" Background="Transparent" BorderThickness="0" Foreground="#565f89" FontSize="11" Margin="15,0,0,0" Cursor="Hand" VerticalAlignment="Center">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}"/>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="#7aa2f7"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>

        <!-- Chart Canvas -->
        <Grid Grid.Row="1" ClipToBounds="True" Background="Transparent">
            
            <!-- Custom High-Perf Renderer -->
            <local:HistogramRenderer x:Name="BarRenderer" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
            
            <!-- Axis Overlay (if needed, or draw in renderer) -->
            <!-- Axis Overlay (if needed, or draw in renderer) -->
            <!-- Removed duplicate AxisCanvas -->


        </Grid>

        <!-- X-Axis Labels -->
        <Canvas x:Name="AxisCanvas" Grid.Row="2" Height="20" ClipToBounds="False"/>
    </Grid>
</UserControl>
