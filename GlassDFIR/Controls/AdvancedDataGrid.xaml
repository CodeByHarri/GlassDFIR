<UserControl x:Class="GlassDFIR.Controls.AdvancedDataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GlassDFIR.Controls"
             xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
             mc:Ignorable="d" 
             PreviewMouseWheel="UserControl_PreviewMouseWheel"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ColumnHeaderGripperStyle" TargetType="Thumb">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="SizeWE"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style to apply TextHighlighter behavior to TextBlocks in cells -->
        <Style x:Key="HighlightedTextBlockStyle" TargetType="TextBlock">
            <Setter Property="local:TextHighlighter.Text" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/>
            <Setter Property="local:TextHighlighter.HighlightText" 
                    Value="{Binding GlobalFilterText, RelativeSource={RelativeSource AncestorType=local:AdvancedDataGrid}}"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="4,0"/>
        </Style>

        <Style x:Key="ColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <!-- Type Indicator -->
                                    <TextBlock Text="{Binding Column.(local:AdvancedDataGrid.ColumnTypeIcon), RelativeSource={RelativeSource TemplatedParent}}" 
                                               FontSize="10" VerticalAlignment="Center" Margin="0,0,6,0"
                                               Foreground="{StaticResource DimmedTextBrush}" Opacity="0.7"/>
                                    <ContentPresenter VerticalAlignment="Center"/>
                                    <!-- Filter Icon Button -->
                                    <Button Click="HeaderFilter_Click" Background="Transparent" BorderThickness="0" Padding="4,0" Cursor="Hand" VerticalAlignment="Center" Margin="4,0,0,0">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Button.Template>
                                        <Path x:Name="FilterPath" Data="M0,0 L10,0 L8,3 L8,10 L2,10 L2,3 Z" Stretch="Uniform" Width="8" Height="8" VerticalAlignment="Center">
                                            <Path.Style>
                                                <Style TargetType="Path">
                                                    <Setter Property="Fill" Value="{StaticResource DimmedTextBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Column.(local:AdvancedDataGrid.IsColumnFiltered), RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                                                            <Setter Property="Fill" Value="#73daca"/> <!-- Teal/Lighter color for active filter -->
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Path.Style>
                                        </Path>
                                    </Button>
                                    <Path x:Name="SortArrow" Visibility="Collapsed" Data="M0,0 L8,0 L4,4 Z" Fill="{StaticResource PrimaryTextBrush}" Margin="8,0,0,0" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5"/>
                                </StackPanel>
                            </Border>
                            <Thumb x:Name="PART_LeftHeaderGripper" HorizontalAlignment="Left" Style="{StaticResource ColumnHeaderGripperStyle}"/>
                            <Thumb x:Name="PART_RightHeaderGripper" HorizontalAlignment="Right" Style="{StaticResource ColumnHeaderGripperStyle}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="SortDirection" Value="Ascending">
                                <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="SortArrow" Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="180"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="SortDirection" Value="Descending">
                                <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="ColumnHeader_PreviewMouseLeftButtonDown"/>
            <EventSetter Event="PreviewMouseRightButtonDown" Handler="ColumnHeader_PreviewMouseRightButtonDown"/>
            <EventSetter Event="PreviewMouseRightButtonUp" Handler="ColumnHeader_PreviewMouseRightButtonUp"/>
            <EventSetter Event="MouseMove" Handler="ColumnHeader_MouseMove"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Distinct Filter Popup Style -->
        <Style x:Key="FilterListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="#1a1b26"/>
            <Setter Property="Foreground" Value="#c0caf5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="MaxHeight" Value="300"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <Style TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="#c0caf5"/>
            <Setter Property="Margin" Value="4,2"/>
        </Style>

        <Style x:Key="CellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SelectionBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <ControlTemplate x:Key="MenuItemControlTemplate" TargetType="{x:Type MenuItem}">
            <Border x:Name="templateRoot" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                <Grid VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                        <ColumnDefinition Width="13"/>
                    </Grid.ColumnDefinitions>
                    <ContentPresenter x:Name="Icon" Content="{TemplateBinding Icon}" ContentSource="Icon" HorizontalAlignment="Center" Height="16" Margin="3" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center" Width="16"/>
                    <ContentPresenter x:Name="menuHeaderContainer" ContentTemplate="{TemplateBinding HeaderTemplate}" Content="{TemplateBinding Header}" Grid.Column="1" ContentStringFormat="{TemplateBinding HeaderStringFormat}" ContentSource="Header" HorizontalAlignment="Left" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                    <TextBlock x:Name="menuGestureText" Text="{TemplateBinding InputGestureText}" Grid.Column="2" DockPanel.Dock="Right" Foreground="#565f89" Margin="5,2,0,2" VerticalAlignment="Center"/>
                    <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}" Placement="Right" VerticalOffset="-3">
                        <Border x:Name="SubMenuBorder" Background="#1a1b26" BorderBrush="#414868" BorderThickness="1" Padding="2">
                            <ScrollViewer x:Name="SubMenuScrollViewer" Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                                <Grid RenderOptions.ClearTypeHint="Enabled">
                                    <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Cycle" Grid.IsSharedSizeScope="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" KeyboardNavigation.TabNavigation="Cycle"/>
                                </Grid>
                            </ScrollViewer>
                        </Border>
                    </Popup>
                </Grid>
            </Border>
             <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" TargetName="templateRoot" Value="#414868"/>
                    <Setter Property="BorderBrush" TargetName="templateRoot" Value="#414868"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Grid.IsSharedSizeScope" Value="True"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="#1a1b26" BorderBrush="#414868" BorderThickness="1" Padding="2">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type MenuItem}">
            <Setter Property="Foreground" Value="#c0caf5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Template" Value="{StaticResource MenuItemControlTemplate}"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Group By Panel -->
            <RowDefinition Height="Auto"/> <!-- Auto-Filter Row Header -->
            <RowDefinition Height="*"/>    <!-- Main Grid -->
        </Grid.RowDefinitions>

        <!-- Timeline Histogram -->
        <local:TimelineHistogram x:Name="TimelineChart" Grid.Row="0" Margin="0,0,0,2" Height="100"
                                 ItemsSource="{Binding ElementName=MainGrid, Path=ItemsSource}"
                                 FilterChanged="TimelineChart_FilterChanged"
                                 DateColumnChanged="TimelineChart_DateColumnChanged"/>
        
        <!-- Auto-Filter Row Header -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,1,1" Padding="0">
        <ScrollViewer x:Name="FilterScrollViewer" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                      HorizontalAlignment="Stretch">
            <ItemsControl x:Name="FilterRow">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="{Binding ActualWidth}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0" Visibility="{Binding Visibility}">
                                <TextBox BorderThickness="0" Background="Transparent" Foreground="{StaticResource PrimaryTextBrush}" Padding="4,2" 
                                         ToolTip="Type to filter..."
                                         TextChanged="FilterTextBox_TextChanged" Tag="{Binding}"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Dynamic Column Headers Template and Main Grid handled in code-behind usually, 
             but we need a place for the popup -->
        <Popup x:Name="DistinctFilterPopup" StaysOpen="False" Placement="Mouse" HorizontalOffset="-10" VerticalOffset="-10" AllowsTransparency="True">
            <Border x:Name="PopupBorder" Background="#1a1b26" BorderBrush="#414868" BorderThickness="1" CornerRadius="4" Padding="8" MinWidth="180" MaxWidth="300" Focusable="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Filter Values" Foreground="#7aa2f7" FontWeight="Bold" Margin="0,0,0,8"/>
                    
                    <CheckBox Grid.Row="1" x:Name="SelectAllCheckBox" Content="(Select All)" IsChecked="True" 
                              Checked="SelectAll_Checked" Unchecked="SelectAll_Unchecked" Margin="0,0,0,5"/>
                    
                    <ListBox Grid.Row="2" x:Name="DistinctValuesList" Style="{StaticResource FilterListBoxStyle}"
                             VirtualizingStackPanel.IsVirtualizing="True"
                             VirtualizingStackPanel.VirtualizationMode="Recycling"
                             ScrollViewer.CanContentScroll="True">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Value}" IsChecked="{Binding IsChecked}" 
                                          Checked="FilterCheck_Changed" Unchecked="FilterCheck_Changed"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Popup>

        <!-- Main DataGrid -->
        <DataGrid x:Name="MainGrid" Grid.Row="2"
                  AutoGenerateColumns="True"
                  AutoGeneratingColumn="MainGrid_AutoGeneratingColumn"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  Sorting="MainGrid_Sorting"
                  LoadingRow="MainGrid_LoadingRow"
                  ScrollViewer.ScrollChanged="MainGrid_ScrollChanged"
                  ContextMenuOpening="MainGrid_ContextMenuOpening"
                  ColumnReordered="MainGrid_ColumnReordered"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  Background="Transparent"
                  BorderThickness="0"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  GridLinesVisibility="None"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  HorizontalScrollBarVisibility="Visible"
                  VerticalScrollBarVisibility="Auto"
                  ScrollViewer.CanContentScroll="True" 
                  MouseDoubleClick="MainGrid_MouseDoubleClick"
                  MinColumnWidth="150"
                  ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}"
                  CellStyle="{StaticResource CellStyle}"
                  AlternationCount="2">

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                    <Setter Property="Background" Value="#1a1b26"/> <!-- Tokyo Night Background -->
                    <Style.Triggers>
                        <Trigger Property="AlternationIndex" Value="1">
                            <Setter Property="Background" Value="#24283b"/> <!-- Tokyo Night Surface -->
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource SelectionBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                             <Setter Property="Background" Value="#414868"/> <!-- Surface Light -->
                             <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="View Value" Click="ViewValue_Click">
                        <MenuItem.Icon><TextBlock Text="👁️"/></MenuItem.Icon>
                    </MenuItem>
                    <Separator/>
                    <MenuItem Header="Add Formatting Rule">
                        <MenuItem Header="Red" Click="AddFormatting_Click" CommandParameter="#f7768e">
                            <MenuItem.Icon><Border Width="12" Height="12" Background="#f7768e" CornerRadius="6"/></MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Grey" Click="AddFormatting_Click" CommandParameter="#565f89">
                            <MenuItem.Icon><Border Width="12" Height="12" Background="#565f89" CornerRadius="6"/></MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Pink" Click="AddFormatting_Click" CommandParameter="#f7768e">
                            <MenuItem.Icon><Border Width="12" Height="12" Background="#f7768e" CornerRadius="6"/></MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Orange" Click="AddFormatting_Click" CommandParameter="#ff9e64">
                            <MenuItem.Icon><Border Width="12" Height="12" Background="#ff9e64" CornerRadius="6"/></MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Yellow" Click="AddFormatting_Click" CommandParameter="#e0af68">
                            <MenuItem.Icon><Border Width="12" Height="12" Background="#e0af68" CornerRadius="6"/></MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Green" Click="AddFormatting_Click" CommandParameter="#9ece6a">
                            <MenuItem.Icon><Border Width="12" Height="12" Background="#9ece6a" CornerRadius="6"/></MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Teal" Click="AddFormatting_Click" CommandParameter="#73daca">
                            <MenuItem.Icon><Border Width="12" Height="12" Background="#73daca" CornerRadius="6"/></MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Cyan" Click="AddFormatting_Click" CommandParameter="#2ac3de">
                            <MenuItem.Icon><Border Width="12" Height="12" Background="#2ac3de" CornerRadius="6"/></MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Blue" Click="AddFormatting_Click" CommandParameter="#7aa2f7">
                            <MenuItem.Icon><Border Width="12" Height="12" Background="#7aa2f7" CornerRadius="6"/></MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Purple" Click="AddFormatting_Click" CommandParameter="#bb9af7">
                            <MenuItem.Icon><Border Width="12" Height="12" Background="#bb9af7" CornerRadius="6"/></MenuItem.Icon>
                        </MenuItem>
                    </MenuItem>
                    <Separator/>
                    <MenuItem Header="Copy to Clipboard" Click="CopyToClipboard_Click"/>
                    <MenuItem Header="Filter for Value" Click="FilterForValue_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </Grid>
</UserControl>
